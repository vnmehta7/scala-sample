db {
  driver = "slick.driver.H2Driver$"

  db {
    url = "jdbc:h2:mem:TEST;INIT=CREATE SCHEMA IF NOT EXISTS TEST;DB_CLOSE_DELAY=-1"
    driver = org.h2.Driver
    connectionPool = disabled
    keepAliveConnection = true
  }
}